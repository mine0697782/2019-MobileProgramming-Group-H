<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grouph.recipelab">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <activity
            android:name=".activity.ResearchResultActivity"
            android:label="@string/label_research_result" />
        <activity
            android:name=".activity.AddResearchActivity"
            android:label="@string/label_add_research" />
        <activity
            android:name=".activity.ResearchingListActivity"
            android:label="@string/label_researching_list" />
        <activity
            android:name=".activity.AddRecipeActivity"
            android:label="@string/label_add_recipe" />
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/label_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>